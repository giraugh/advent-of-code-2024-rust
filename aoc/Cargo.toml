[package]
name = "aoc"
version = "0.1.0"
edition = "2021"

[dependencies]
derive_more = { version = "1.0.0", features = ["add", "add_assign", "from", "into", "mul", "mul_assign"] }
itertools.workspace = true
